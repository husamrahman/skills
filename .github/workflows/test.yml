name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  isolation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run skill isolation tests
        run: bash tests/run.sh
